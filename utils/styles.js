import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  tab: {
    backgroundColor: 'black',
    padding: 5
  },
  noDataText: {
    fontSize: 20,
    paddingTop: 20,
    paddingBottom: 20
  }
});

export { styles };
